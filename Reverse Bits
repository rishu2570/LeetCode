class Solution:
    def reverseBits(self, n: int) -> int:
        result = 0

        for i in range(32):
            bit = n % 2          # get last bit
            result = result * 2 + bit
            n = n // 2          # remove last bit

        return result
